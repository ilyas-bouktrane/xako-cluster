apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: omada-secret
  namespace: omada
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: omada-secret
  dataFrom:
  - extract:
      key: omada
